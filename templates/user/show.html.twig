{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>Mon Profil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ user.surname }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ user.name }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ user.pseudo }}</td>
            </tr>          
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            
            {# <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr> #}
            <tr>
                <th>Classement</th>
                <td>{{ user.ranking }}</td>
            </tr>
            <tr>
                <th>Avatar</th>
                <td>{{ user.avatar }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a>

    <button>Demander la suppression de mon compte</button>

    {% if "ROLE_ADMIN" in app.user.getRoles  %}
		{{ include('user/_delete_form.html.twig') }}
	{% endif %}
{% endblock %}
